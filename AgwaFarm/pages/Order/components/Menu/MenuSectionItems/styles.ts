import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    wrapper: {
        display: 'flex',
        flexDirection: 'row',
    },
    menuItem: {
        width: 75,
        height: 'auto',
        padding: 11.25,
        display: 'flex',
        justifyContent: 'flex-start',
        alignItems: 'center',
    }
})