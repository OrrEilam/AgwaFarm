import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    wrapper: {
        display: 'flex',
        flexDirection: 'row',
    },
    menuItem: {
        width: '5rem',
        height: 'auto',
        padding: '0.75rem',
        display: 'flex',
        justifyContent: 'flex-start',
        alignItems: 'center',
    }
})