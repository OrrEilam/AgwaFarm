import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    selectorItem: {
        flexBasis: '20%',
        padding: '0.25rem',
        display: 'flex',
        justifyContent: 'flex-start',
        alignItems: 'center',
    },
});