/**
 * An enum for icons-names, to prevent the usage of magic-strings.
 */
export enum IconName {
    MinusSign = 'minus-sign',
    PlusSign = 'plus-sign',
    QuestionMark = 'question-mark',
    XSign = 'x-sign',
}